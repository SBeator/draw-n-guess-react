const prefix = 'PAINT_'

const DRAWING = `${prefix}DRAWING`
const SHOW_LINE = `${prefix}SHOW_LINE`
const CHANGE_PAINT_METHOD = `${prefix}CHANGE_PAINT_METHOD`

export { DRAWING, SHOW_LINE, CHANGE_PAINT_METHOD }
