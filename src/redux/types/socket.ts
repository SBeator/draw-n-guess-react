const prefix = 'SOCKET_';

const CONNECTING = `${prefix}CONNECTING`;
const CONNECTED = `${prefix}CONNECTED`;
const CONNECTED_ERROR = `${prefix}CONNECTED_ERROR`;

export { CONNECTING, CONNECTED, CONNECTED_ERROR };
